# C5 Result

Accepted: index rebuild completion publishes a terminal running-marker barrier before the terminal task object, then clears persisted and in-memory dedup state while holding the task mutex. Readers treat a terminal marker as authoritative and do not fall back to a stale running task object.

Verification: `go test -race -mod=readonly ./internal/storage -run TestStartIndexRebuildDeduplicatesRunningTenantTask -count=100` passes.
