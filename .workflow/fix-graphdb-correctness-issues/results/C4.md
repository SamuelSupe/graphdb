# C4 Result

Accepted: non-dry-run restore drills reject the live tenant namespace and any non-empty target, claim an empty target with the writer lease, restore with overwrite disabled, and capture exact non-control object ETags. Cleanup verifies the ownership marker and unchanged object set under the lease before purge.

Verification: existing target and live namespace remain unchanged, a post-capture mutation blocks cleanup, and owned cleanup removes only the drill tenant including its temporary purge tombstone.
