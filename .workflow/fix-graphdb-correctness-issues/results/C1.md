# C1 Result

Accepted: GC validates a sharded snapshot cursor against the current manifest before any cleanup begins. A cursor inside the current snapshot returns an error without deleting commit or snapshot objects.

Verification: targeted current-cursor, normal checkpoint-resume and old-snapshot cleanup tests pass.
