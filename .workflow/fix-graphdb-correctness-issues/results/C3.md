# C3 Result

Accepted: direct commits reserve an idempotency record before mutation and persist a prepared result before manifest publication. Ambiguous manifest responses and failed finalization are recovered by verifying the exact commit identity; a writer takeover clears tenant and object caches.

Verification: reservation timeout leaves version zero, write-then-error manifest publication replays version one, failed finalization replays without a second commit, and a second writer recovers the prepared record after lease takeover.
