# C3: Direct commit idempotency

Objective: recover an idempotent result when graph publication succeeds but the separate record write fails, without trusting indefinite negative existence caches.

Ownership: direct commit idempotency, commit durable metadata, object key cache and tests.

Verification: after-apply failure retry, mismatched-body rejection and two-store failover tests.
