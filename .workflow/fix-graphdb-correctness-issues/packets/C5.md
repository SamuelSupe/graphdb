# C5: Index rebuild terminal race

Objective: publish terminal task state and in-memory dedup state atomically enough that a completed task is never reused.

Ownership: index task state and its race regression.

Verification: targeted test under `-race -count=100`.
