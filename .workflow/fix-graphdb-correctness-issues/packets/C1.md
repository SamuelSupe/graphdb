# C1: GC checkpoint safety

Objective: reject or safely handle a checkpoint cursor that points into the current sharded snapshot.

Ownership: `internal/storage/gc*.go`, `internal/storage/gc*_test.go`, minimal HTTP validation if required.

Verification: malicious/current cursor regression plus existing checkpoint pagination tests.
