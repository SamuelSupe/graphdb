[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "graphdb-sdk"
version = "1.1.5"
description = "Python SDK for GGraphDB HTTP API"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["graphdb_sdk*"]
