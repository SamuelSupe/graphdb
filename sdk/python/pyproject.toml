[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "graphdb-sdk"
version = "0.1.0"
description = "Python SDK for GraphDB HTTP API"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["graphdb_sdk*"]
